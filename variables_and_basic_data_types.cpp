#include <iostream>

int main()
{
    //integer (whole number)
    int x = 5;
    int y = 6;
    int age = 24;

    //double (when decimal neededd)
    double z = 7.5;

    //char (can only store single character)
    char grade = 'A';
    char initial = 'B';
    char currency = '$';

    //bool (true or false)
    bool student = false;
    bool power = true;
    bool forSale = true;

    //string (an object that represents a sequence of text) (char but more than 1 character)
    std::string name = "KZA";
    std::string day = "Friday";
    std::string food = "Hamburger";
    std::string address = "123 Fake Street";

    return 0;
}